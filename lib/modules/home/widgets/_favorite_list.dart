part of '../home.dart';

class _FavoriteList extends StatelessWidget {
  const _FavoriteList();

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('fani')],
    );
  }
}
